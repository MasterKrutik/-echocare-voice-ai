import LandingPage from '@/components/LandingPage';

/**
 * Public Citizen Voice Intake Route (/call)
 *
 * Provides the interactive Agora Conversational AI intake experience for citizens.
 * Completely public — no login or authentication required.
 */
export default function CallPage() {
  return <LandingPage />;
}
